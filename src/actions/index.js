//import {filterLoading,filterLoaded,filterError} from '../components/heroesFilters/filtersSlice';
// export const heroesFetching = createAction('HEROES_FETCHING');

// export const heroesFetched = createAction('HEROES_FETCHED');

// export const heroesFetchingError = createAction('HEROES_FETCHING_ERROR');

// export const heroDeleted = createAction('HERO_DELETED');

// export const heroCreated = createAction('HERO_CREATED');

// export const filterLoading = createAction('FILTER_LOADING');

// export const filterLoaded = createAction('FILTER_LOADED');

// export const filterError = createAction('FILTER_ERROR');

// export const filterChange = createAction('FILTER_CHANGE');


// export const fetchHeroes = (request) => (dispatch) => {
//     dispatch(heroesFetching());
//         request("http://localhost:3001/heroes")
//             .then(data => dispatch(heroesFetched(data)))
//             .catch(() => dispatch(heroesFetchingError()))
// }

// export const fetchFilters = (request) => (dispatch) => {
//     dispatch(filterLoading());
//     request("http://localhost:3001/filters")
//         .then(data => dispatch(filterLoaded(data)))
//         .catch(() => dispatch(filterError()))
// }


